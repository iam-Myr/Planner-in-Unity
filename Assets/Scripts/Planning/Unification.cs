using Planning;
using System.Collections.Generic;

public static class Unification
{

    // pointA.BindTo(pointB) to actually bind variables!
    // Make it so I can bind compounds, lists, vars






















    // Checks if two predicates can be unified
    //public static bool CanUnify(Predicate p1, Predicate p2)
    //{
      //  if (p1.func.Method.Name != p2.func.Method.Name) return false;
        //if (p1.args.Count != p2.args.Count) return false;

        //for (int i = 0; i < p1.args.Count; i++)
        //{
          //  var a = p1.args[i].Get();
           // var b = p2.args[i].Get();

            //if (a != null && b != null && !a.Equals(b))
             //   return false;
       // }

        //return true;
    //}

    // Unifies two predicates by binding their arguments
    //public static void Unify(Predicate p1, Predicate p2)
    //{
      //  for (int i = 0; i < p1.args.Count; i++)
        //{
          //  Pointer a = p1.args[i];
            //Pointer b = p2.args[i];
            //
            //if (a != b)
            //{
              //  a.BindTo(b);
           // }
        //}
    //}
}